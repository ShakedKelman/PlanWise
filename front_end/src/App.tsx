
import React, { useEffect } from 'react';

const App: React.FC = () => {
//   const { auth } = useSelector((state: RootState) => state);

  return (
    <div>
  
    </div>
  );
};

export default App;
